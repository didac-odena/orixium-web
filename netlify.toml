[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback: let React Router handle client-side routes on refresh / deep links.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
